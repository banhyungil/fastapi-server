ALTER TABLE public.t_file
  ADD COLUMN width  int4 NULL,
  ADD COLUMN height int4 NULL;

COMMENT ON COLUMN public.t_file.width IS '이미지 가로 해상도 (px)';
COMMENT ON COLUMN public.t_file.height IS '이미지 세로 해상도 (px)';
