# Image Processing Pipeline System Design

## 1. 개요 (Overview)

본 시스템은 원본 이미지를 손상시키지 않고 사용자 정의 알고리즘을 순차적으로 적용하는 **비파괴적 편집(Non-destructive Editing)** 방식을 채택합니다. 모든 처리 단계는 메타데이터로 관리되며, 필요 시 실시간으로 재연산하여 결과를 출력합니다.

## 2. 핵심 아키텍처 (Core Architecture)

### 2.1 데이터 보존 전략

- **Source of Truth**: 원본 이미지(`t_file`)와 알고리즘 파라미터(`t_process_step`)를 최우선 데이터로 간주합니다.
- **최종 결과물**: 연산 효율을 위해 최종 처리된 이미지만 물리 파일로 저장하며, 중간 단계 이미지는 저장하지 않습니다.
- **재연산 메커니즘**: 사용자가 중간 단계 수정을 요청하면, 원본 파일에 `step_order` 순으로 파라미터를 재적용하여 새로운 결과물을 생성합니다.

### 2.2 테이블 역할 정의

| 테이블명 | 역할 | 주요 데이터 |
|---|---|---|
| `t_file` | 물리 파일 정보 관리 | 파일 경로, 이름, 크기, MIME 타입 |
| `t_image_process` | 편집 세션 관리 | 원본-결과물 매핑, 작업 상태 |
| `t_process_step` | 알고리즘 레시피 | 순서(`step_order`), 파라미터(`jsonb`), 활성상태 |

## 3. 상세 프로세스 (Workflow)

### 3.1 이미지 처리 및 저장

1. 사용자가 화면에서 알고리즘 및 옵션 설정.
2. UI 레이어: 저해상도 프록시 이미지로 실시간 재연산 결과 노출.
3. 저장 시:
   - 원본 해상도 이미지에 전체 파이프라인 적용.
   - 결과물을 새로운 파일로 `t_file`에 저장.
   - `t_image_process` 및 `t_process_step`에 레시피 기록.

### 3.2 단계별 원복 및 수정 (Undo/Edit)

1. 특정 단계의 `is_enabled`를 `false`로 변경하거나 `parameters` 수정.
2. 수정된 단계부터 마지막 단계까지 순차적으로 재연산 수행.
3. 원본 이미지는 변경되지 않으므로 언제든지 초기 상태로 복구 가능.

## 4. 데이터 활용 예시 (JSON Structure)

`t_process_step.parameters` 필드에는 다음과 같은 형식의 데이터가 저장됩니다 (Pydantic 모델 직렬화 결과).

```json
{
  "kernel_size": 5,
  "sigma": 1.2,
  "border_type": "BORDER_REPLICATE"
}
```

## 5. 향후 고려 사항 (Scalability)

- **중간 결과 캐싱**: 연산량이 매우 큰 알고리즘(AI 기반)이 포함될 경우, 해당 단계의 결과물을 임시 스토리지에 캐싱하여 재연산 속도를 최적화할 수 있습니다.
- **버전 관리**: `t_image_process`의 `is_latest` 필드를 활용하여 사용자가 작업했던 여러 버전의 편집 이력을 모두 보존할 수 있습니다.

