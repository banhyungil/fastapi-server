- api 레이어 단에만 pydantic 스키마를 활용한다
- 요청, 응답에만 검증하는걸로
- service, repo는 꼭 요청 응답과 동일 schema가 아닐수있기 때문이다.
- 그렇다하더라도 redunduncy가 있는건사실..
- pydantic schema는 별도 schemas에서 관리하는데 service, repo 타입은 어디서?
- 각 파일에서 관리하는게 나을려나..
