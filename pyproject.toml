[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"   # @pytest.mark.asyncio 데코레이터 자동 추가

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "W"]  # 에러, 미사용, import정렬, 경고